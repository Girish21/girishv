---
import type { HTMLTag, Polymorphic } from 'astro/types'
import { twMerge } from '../util'

type Props<Tag extends HTMLTag> = Polymorphic<{ as: Tag }>

const { class: pClass = '', as: Element = 'h3', ...props } = Astro.props
---

<Element class={twMerge('text-gray-200 text-subheading', pClass)} {...props}>
  <slot />
</Element>
