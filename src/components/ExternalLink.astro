---
import type { HTMLAttributes } from 'astro/types'

const {
  class: pClass = '',
  rel = 'noopener noreferrer',
  target = '_blank',
  ...props
} = Astro.props

export interface Props extends HTMLAttributes<'a'> {}
---

<a class={pClass} rel={rel} target={target} {...props}>
  <slot />
</a>
